<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Incremental_Processed_as_RIO</fullName>
    <active>true</active>
    <description>If incremental is added to IO, the opportunity stage needs to equal Processed as RIO</description>
    <errorConditionFormula>AND(

$Setup.byPass__c.ByPass_Validation__c = FALSE,

RecordType.DeveloperName = $Label.OPP_RECORDTYPEDEVNAME_REVISION,

ISPICKVAL(StageName, &#39;Closed Won&#39;)

)</errorConditionFormula>
    <errorMessage>Cannot change Incremental stage to Closed Won. Please change it &#39;Processed as RIO&#39;</errorMessage>
</ValidationRule>
